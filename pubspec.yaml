name: instagram_clone
description: Instagarm Clone Using FLutter
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_staggered_grid_view: ^0.4.0
  shimmer: ^2.0.0
  video_player: ^2.2.2
#  image_picker: ^0.8.4
#  firebase: ^9.0.1
#  firebase_core: ^1.6.0
#  firebase_analytics: ^8.3.1
#  firebase_auth: ^3.1.0
#  cloud_firestore: ^2.5.1
#  firebase_storage: ^10.0.3
#  http:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/

  fonts:
    - family: text
      fonts:
        - asset: fonts/Poppins-Regular.ttf